name: Java Build, Test & Trivy Scan

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: sai  # your self-hosted runner

    steps:

      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Verify Java, Maven, and Trivy versions
      - name: Verify Versions
        run: |
          echo "Java version:"
          java -version
          echo "Maven version:"
          mvn -version
          echo "Trivy version:"
          trivy --version

      # Step 3: Compile the project
      - name: Compile with Maven
        run: mvn clean compile

      # Step 4: Run Maven tests
      - name: Run Maven tests
        run: mvn test

      # Step 5: Trivy File System Scan
      - name: Trivy FS Scan
        run: |
          echo "Starting Trivy FS scan..."
          trivy fs --format table .
          trivy fs --format json -o trivy-fs-report.json .
          echo "Trivy FS scan JSON report generated: trivy-fs-report.json"

      # Step 6: Fail on high/critical vulnerabilities
      - name: Fail on High/Critical Vulnerabilities
        run: |
          echo "Checking for high or critical vulnerabilities..."
          trivy fs --severity HIGH,CRITICAL --exit-code 1 --quiet .

      # Step 7: Upload Trivy JSON Report (always runs)
      - name: Upload Trivy JSON Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-fs-report
          path: trivy-fs-report.json

      # Step 8: Package the project
      - name: Package with Maven
        run: mvn package -DskipTests
